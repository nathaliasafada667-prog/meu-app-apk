import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.esmael.store',
  appName: 'Esmael Store',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
